function Tag({value}) {
    return(
        <div className="tag">{value}</div>

        // <>
        //     {tags.map((tag) => (
        //         <div>{tag}</div>
        //     ))}
        // </>


    //     <div className="tag">
    //     <span className="tag-name">{tagName}</span>
    //   </div>
    )
}

export default Tag